`cli-64.exe` / `cli-32.exe` are the console-launcher stub binaries distributed
with `setuptools` (MIT licensed) — real, native-compiled Windows PE
executables (not managed/.NET), used here purely as PE-format test fixtures
for the loader/scanner. Source: https://github.com/pypa/setuptools
